def tax(x, r):
    """
    Returns the amount of tax to be paid on an amount of money x at
    tax rate r.

    """

    pass

def mean(li):
    """
    Returns the mean (or average) of a list of numbers li. Recall
    that the mean of a_1, a_2, ..., a_n is (a_1 + a_2 + ... + a_n) /
    n. We define the mean of an empty list as zero.

    NOTE: Remember that Python uses integer division when integers are
    supplied; in particular, 10 / len([1, 2, 3]) == 3. Use
    float(len(...)) to get a decimal value.

    """

    pass

def roots(a, b, c):
    """
    Returns the roots of the quadratic polynomial a x^2 + b x + c;
    recall the quadratic formula x = (-b +- (b^2 - 4 * a * c)^.5) / (2
    a). Should returna tuple of (x1, x2), where x1 is the smaller of
    the two roots.

    NOTE: a to the power of b is expressed as a ** b, not a^b (a^b is
    a bitwise operator).

    """

    pass

def greeting(name):
    """
    Returns the greeting "Hello, <name>!" were <name> is replaced by
    the contents of the string name.

    """

    pass

def map(f, li):
    """
    Returns a list of elements formed by applying function f to each
    element of li.

    NOTE: This is, of course, a one-liner using the built-in map
    function. Try solving this without it!

    """

    pass

def reduce(predicate, li):
    """
    Returns a list of all elements x of li such that predicate(x) is
    True.

    NOTE: This is, of course, a one-liner using the built-in reduce
    function. Try solving this without it!

    """

    pass
